---
name: claude-roblox-initial-document-check
description: 既存のRoblox設計文書群を実装開始前に監査し、AI実装に耐える状態か判定する。二重正本・矛盾・参照切れ・実装可能性ギャップ・過剰厳密化を検出し、正本境界を保ったまま是正する。Robloxプロジェクトの設計書・GDD・詳細設計・データ定義・テスト仕様をレビューしたい、引き継いだ文書が実装に使えるか確認したい、AI開発用の文書群に整えたい、文書間の矛盾を洗い出したい、実装前チェックをしたい、といった話が出たら必ずこのskillを使う。「docsを見て」「設計書レビューして」「この文書で実装できる？」「文書整えて」のような曖昧な依頼も対象。実装フェーズに入る前の最初のステップとして使う。
---

# Roblox 実装前 文書監査

設計文書が「読める」ことと「AIが実装できる」ことは別。この監査は後者を判定する。

実運用で、一見よく整備された文書群から次が見つかった。ルートの必須3ファイルが削除されていて30箇所以上の参照が切れていた（この状態では最初のテストが定義上必ず失敗する）。ツールチェーンがWindows専用APIを前提にしていたが開発機はmacOSだった。12個の決定ペアが定義されているのに、実際に有効化できる仕組みは3個分しかなかった。素材の制作主体をAIに割り当てているのに、AIが3Dモデルを作る手段がどこにも定義されていなかった。

いずれも文書を通読するだけでは気づきにくく、実装を始めてから詰まる類のもの。

## 1. まず状態を把握する

監査対象を確定し、リポジトリの現状を証拠付きで確認する。名前から推測しない。

```bash
git status --short          # 削除・未追跡ファイル。参照切れの主因になる
git log --oneline -5
find . -type f -name "*.md" -not -path "./.git/*" | head -30
wc -l docs/*.md
```

**削除されたファイルは `git show HEAD:<path>` で内容を確認する。** 作業ツリーから消えていても、文書がそれを参照しているなら実質的な参照切れ。復元すべきか判断が要る。

**コードや成果物が無いこと自体は欠陥ではない。** 文書が記述する成果物（config・scripts・tests・src）が存在しなくても、実装未着手なら正常。ここを間違えると、正常な状態が大量の重大指摘として返る。

判定の分かれ目は「文書が壊れているか」対「まだ作っていないか」。

| 状況 | 扱い |
|---|---|
| `scripts/build.sh` が無い。実装未着手 | **正常**。観察として1行記録する程度 |
| 文書が「`scripts/verify.ps1` を唯一の検証入口とする」と規定し、そのOSが実環境と違う | **欠陥**。手段が実行不能 |
| `src/` が空 | **正常** |
| テストが root `CLAUDE.md` を検査するが、どの文書もその内容を所有していない | **欠陥**。仕様の穴であって未実装ではない |

見分け方: **実装すれば解消するなら正常。実装しても解消しない（何を作ればいいか決まらない）なら欠陥。**

## 2. 監査を走らせる

まず全体を読む監査を1本、その結果を踏まえて観点別の監査を並列で走らせる。

**第1段階 — リポジトリ監査**: 全文書を精読し、各文書が何の正本か、二重正本はどこか、矛盾・参照切れ・未解決状態はどこか、実装可能性のギャップは何かを構造化して報告させる。`repository-auditor` があればそれを使う。読み取り専用で走らせ、ファイルは変更させない。

**第2段階 — 3系統の並列監査**: 第1段階の所見を渡し、観点を分けて同時に走らせる。

| 観点 | 見るもの |
|---|---|
| 整合性 | 二重正本、参照切れ、版の不一致、状態タグの残存、変更伝播の欠落 |
| clean-room | 過去の会話を知らないAIが、文書だけで最初の実装単位を開始できるか |
| Roblox readiness | サーバー権威、Remote検証、永続化、課金、性能予算、モバイル入力、公開・復旧 |

clean-roomは特に効く。「この文書を初めて読む人が着手できるか」は、書いた本人には見えない。

各監査には、指摘を根拠付きで Critical / Major / Minor に分けて出させる。**監査者に修正させない。** 指摘と修正を同じ主体がやると、指摘が甘くなる。

第2段階には、第1段階の所見を渡したうえで**(A)追認 (B)反証・再分類 (C)新規発見**に分けて出させる。これをやらないと第2段階が第1段階のエコーになり、並列で走らせた意味が消える。実運用では、この分類を課したことで第1段階の所見が4件訂正された（「データが敵2種を定義している」は列挙からの推測で、真の欠陥は「敵ロースターの所有者がどの文書にもいない」ことだった）。

詳しい観点は `references/audit-dimensions.md`。

## 3. 過剰厳密化を見分ける

AI生成の設計文書には、目的に対して手段が過大になる傾向がある。この監査で最も価値のある判断がここ。

**兆候**: 実装がまだ存在しない段階で、そのコードの詳細（データ構造のフィールド、ハンドルの扱い、プロセス制御の手順）まで文書が決めている。1つの目的のために何十行もの検証手順が書かれている。手段が特定OSやAPIに強く依存している。

**判定方法**: その仕組みが**何を保証するためにあるか**を1〜3個に言語化する。次に、その目的が手段の1割程度のコストで達成できないかを考える。達成できるなら過剰。

実例: ある文書は「lintの標準ライブラリが固定され、CIがネットワークに依存しない決定論」を保証するために、OS固有のサンドボックス機構・ファイルディスクリプタ継承の遮断証明・プロセスグループの終了確認まで規定していた。目的3点は「設定ファイルのハッシュ一致・ツールのバージョンとバイナリハッシュ照合・CIコマンド列に取得コマンドを含めない契約」で達成できた。

**ただし勝手に削らない。** 過剰厳密化は指摘として報告し、簡素化の是非はユーザーが決める。統制を落とす判断だから。

## 4. 所見を報告する

**所見の件数は品質ではない。** 監査は「見つけた数」で評価されがちだが、読む側が必要とするのは「何から手を付けるか」。症状を並べるほど、その判断は難しくなる。

報告を書く前に、所見を根本原因ごとにまとめる。実運用で、116行の文書群に対して重大所見12件を出したことがある。うち11件は1つの構造的原因（正本境界が宣言だけで機能しておらず、ID参照が実際には解決していない）の症状だった。値の矛盾を個別に直しても再発する。この場合、根本1件＋その症状として11件を束ねるほうが、読み手は正しく動ける。

報告の分量は対象に見合わせる。小さな文書群に長大な報告を返すと、重要な指摘が埋もれる。

監査結果は**状態記録**であって正本ではない。この位置づけを報告書自身に書いておく。矛盾したときは各正本が優先する。

含めるもの:
- 重大所見（対処が必須なもの）— 根拠、影響、対処方針
- 軽微所見（この場で直せるもの）
- 観察（対処不要、記録のみ）
- ギャップマップ — 標準的な文書体系に対して何が存在し、何が分散所有され、何が欠落しているか
- **決定要求** — ユーザーが決めないと進めない項目。選択肢と推奨を添える

未決は `[OPEN] blocking: yes|no` として残す。`blocking: yes` なら進捗記録のblocker欄にも登録する。片方だけだと追跡が切れる。

書式は `references/findings-report.md`。

## 5. 是正する

**独立文書を安易に新設しない。** 標準的な文書体系（HUMAN_ACTIONS、CHANGELOG、DECISIONS等）が「無い」ように見えても、既存文書が機能を分担していることが多い。新設すると二重正本になる。

判断: その事実を今どの文書が所有しているか。所有者がいるなら追記する。いないなら新設する。索引に「分散所有マップ」を作っておくと、次に見た人が同じ判断を繰り返さずに済む。

是正後は監査を再実行し、重大指摘が0になるまで繰り返す。**再監査では前回PASSした観点の再精読を不要と伝える**と時間が短縮される。

## 6. 監査が発散したら

再監査のたびに新しい重大指摘が出続けるなら発散している。実運用で3巡（Major 6→3→5）発散した。

原因はほぼ「実装前に実装詳細を文書で完全に閉じようとしている」。実際に書いてみないと分からないことを先に決めようとすると終わらない。

対処は§3と同じ。目的まで戻り、手段を簡素化する案をユーザーへ出す。この判断をした後、指摘は3→1→0と単調に収束した。

逆に、指摘が実害（テストが定義上必ず落ちる、pinが実効を持たない、存在しないオプションへの依存）を指しているなら、巡が増えても健全。素直に是正する。
